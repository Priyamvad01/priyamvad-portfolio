# Styles

Global design tokens live in `app/globals.css` because Tailwind CSS v4 reads theme variables directly from CSS.

Use this folder for future shared CSS modules, third-party style bridges, or route-specific style documentation.
